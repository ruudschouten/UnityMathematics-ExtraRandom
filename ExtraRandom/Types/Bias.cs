namespace ExtraRandom.Types
{
    /// <summary>
    /// Enum class which determines what kind of result <see cref="BiasedRandom"/> will return when generating a number.
    /// </summary>
    public enum Bias
    {
        Higher,
        Lower
    }
}